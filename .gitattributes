# Auto detect text files and perform LF normalization
* text=auto

# Windows batch files MUST keep CRLF line endings:
# cmd.exe cannot parse a batch file that uses bare LF.
*.bat text eol=crlf
*.cmd text eol=crlf
